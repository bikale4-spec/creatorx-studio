import { useState } from 'react';
import { AudioLines, Check, Mic, Pause, Play, RotateCcw, Save, SlidersHorizontal, Sparkles, Volume2 } from 'lucide-react';
import SectionHeader from '@/components/SectionHeader';
export default function VoiceStudioView() { const [recording, setRecording] = useState(false); const [playing, setPlaying] = useState(false); const [noise, setNoise] = useState(true); const bars = Array.from({ length: 64 }, (_, index) => 18 + ((index * 17) % 42)); return <div className="space-y-7 animate-fade-in-up"><SectionHeader eyebrow="Audio production" title="Voice studio" description="Record, clean, and shape your voice for videos, streams, and podcasts." action={<button className="neon-button-primary px-4 py-2.5 rounded-xl text-sm inline-flex items-center gap-2"><Save className="w-4 h-4" /> Save audio</button>} /><div className="grid lg:grid-cols-[1fr_300px] gap-5"><div className="space-y-5"><div className="glass-card p-6 sm:p-10 text-center"><div className="w-20 h-20 rounded-full bg-cyan-400/10 border border-cyan-400/20 flex items-center justify-center mx-auto mb-6"><Mic className={`w-8 h-8 text-cyan-300 ${recording ? 'animate-pulse' : ''}`} /></div><h3 className="text-lg font-semibold">{recording ? 'Recording your voice...' : 'Ready when you are'}</h3><p className="text-xs text-gray-600 mt-2">{recording ? 'Speak naturally. Your waveform will appear below.' : 'Record a clean take with your microphone'}</p><button onClick={() => setRecording((value) => !value)} className={`${recording ? 'bg-red-500/15 text-red-300 border border-red-500/30' : 'neon-button-primary'} mt-6 px-5 py-3 rounded-xl text-sm inline-flex items-center gap-2`}>{recording ? <><Pause className="w-4 h-4" /> Stop recording</> : <><Mic className="w-4 h-4" /> Start recording</>}</button></div><div className="glass-card p-4 sm:p-6"><div className="flex items-center justify-between mb-4"><div className="flex items-center gap-2"><AudioLines className="w-4 h-4 text-cyan-300" /><span className="text-xs font-semibold">Audio waveform</span></div><span className="text-[10px] text-gray-600">00:00:00</span></div><div className="h-28 rounded-xl bg-white/[0.02] flex items-center justify-center gap-[3px] px-3 overflow-hidden">{bars.map((height, index) => <div key={index} className={`w-1 rounded-full ${recording ? 'bg-cyan-400 wave-bar' : 'bg-cyan-400/40'}`} style={{ height: `${height}%`, animationDelay: `${index * 0.02}s` }} />)}</div><div className="flex items-center justify-center gap-4 mt-5"><button onClick={() => setPlaying((value) => !value)} className="w-9 h-9 rounded-full neon-button flex items-center justify-center">{playing ? <Pause className="w-4 h-4" /> : <Play className="w-4 h-4 ml-0.5" />}</button><button className="text-gray-600 hover:text-white"><RotateCcw className="w-4 h-4" /></button></div></div></div><aside className="glass-card p-5 h-fit"><div className="flex items-center gap-2 mb-5"><SlidersHorizontal className="w-4 h-4 text-purple-300" /><h3 className="text-sm font-semibold">Voice effects</h3></div><div className="space-y-5"><label className="block text-xs text-gray-500">Volume <input type="range" defaultValue="80" className="w-full mt-3 accent-cyan-400" /></label><label className="block text-xs text-gray-500">Pitch <input type="range" defaultValue="50" className="w-full mt-3 accent-purple-400" /></label><label className="block text-xs text-gray-500">Echo <input type="range" defaultValue="10" className="w-full mt-3 accent-pink-400" /></label><div className="flex items-center justify-between py-2"><div className="flex items-center gap-2"><Sparkles className="w-4 h-4 text-cyan-300" /><span className="text-xs text-gray-400">Noise reduction</span></div><button onClick={() => setNoise((value) => !value)} className={`w-10 h-5 rounded-full ${noise ? 'bg-cyan-400' : 'bg-gray-700'}`}><span className={`block w-3.5 h-3.5 rounded-full bg-white transition-transform ${noise ? 'translate-x-5' : 'translate-x-0.5'}`} /></button></div></div><div className="mt-6 pt-4 border-t border-white/5 text-[11px] text-gray-600 flex items-center gap-2"><Volume2 className="w-3 h-3" /> Input: Default microphone <Check className="w-3 h-3 text-green-400 ml-auto" /></div></aside></div></div>;
}
