import { useState } from 'react';
import { Check, ImagePlus, Layers, Mic2, Sparkles, Upload, UserCircle, Wand2 } from 'lucide-react';
import { supabase } from '@/lib/supabase';
import SectionHeader from '@/components/SectionHeader';
export default function AvatarStudioView() { const [uploaded, setUploaded] = useState(false); const [name, setName] = useState(''); const [saved, setSaved] = useState(false); async function save() { if (!name.trim()) return; await supabase.from('avatars').insert({ name: name.trim(), status: 'active' }); setSaved(true); } return <div className="space-y-7 animate-fade-in-up"><SectionHeader eyebrow="Virtual identity" title="Avatar studio" description="Create a visual identity that feels unmistakably yours." /><div className="grid lg:grid-cols-[1fr_360px] gap-5"><div className="glass-card p-5 sm:p-8"><div className="aspect-[4/3] rounded-2xl bg-[#08080c] border border-white/5 flex items-center justify-center relative overflow-hidden"><div className="absolute inset-0 bg-grid opacity-30" />{uploaded ? <div className="relative text-center"><div className="w-36 h-36 rounded-full bg-gradient-to-br from-cyan-400/30 to-purple-500/30 border-2 border-cyan-400/40 flex items-center justify-center mx-auto mb-4"><UserCircle className="w-20 h-20 text-cyan-300" /></div><p className="text-sm text-gray-400">Avatar preview</p></div> : <div className="relative text-center"><div className="w-24 h-24 rounded-full bg-white/[0.03] flex items-center justify-center mx-auto mb-4"><UserCircle className="w-12 h-12 text-gray-700" /></div><p className="text-sm text-gray-500">Upload a portrait to begin</p><label className="mt-4 inline-flex items-center gap-2 neon-button px-4 py-2 rounded-xl text-xs cursor-pointer"><Upload className="w-3 h-3" /> Upload avatar<input type="file" accept="image/*" className="hidden" onChange={() => setUploaded(true)} /></label></div>}<span className="absolute top-4 left-4 text-[10px] text-gray-600 uppercase tracking-widest">Preview stage</span></div><div className="grid grid-cols-3 gap-2 mt-4"><button className="p-3 rounded-xl bg-cyan-400/10 border border-cyan-400/20 text-cyan-300 text-xs"><ImagePlus className="w-4 h-4 mx-auto mb-1" /> Portrait</button><button className="p-3 rounded-xl bg-white/[0.03] text-gray-500 text-xs"><Layers className="w-4 h-4 mx-auto mb-1" /> Background</button><button className="p-3 rounded-xl bg-white/[0.03] text-gray-500 text-xs"><Mic2 className="w-4 h-4 mx-auto mb-1" /> Voice</button></div></div><aside className="glass-card p-5 h-fit"><div className="flex items-center gap-2 mb-5"><Sparkles className="w-4 h-4 text-cyan-300" /><h3 className="text-sm font-semibold">Avatar setup</h3></div><label className="block text-xs text-gray-400">Avatar name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Nova" className="input-field w-full mt-2 px-4 py-3 text-sm" /></label><div className="mt-5 space-y-3"><button className="w-full neon-button py-3 rounded-xl text-xs inline-flex items-center justify-center gap-2"><Wand2 className="w-4 h-4" /> Enhance appearance</button><button onClick={() => void save()} className="w-full neon-button-primary py-3 rounded-xl text-xs inline-flex items-center justify-center gap-2">{saved ? <><Check className="w-4 h-4" /> Saved to studio</> : 'Save avatar'}</button></div><div className="mt-6 pt-4 border-t border-white/5 text-[11px] text-gray-600 leading-relaxed">Use your avatar in streams, videos, and AI-powered presentations.</div></aside></div></div>;
}
