import { useState } from 'react';
import { Crop, Film, Gauge, Layers, Maximize, Music, Play, Redo2, RotateCw, Scissors, SlidersHorizontal, Sparkles, Split, Sticker, Undo2, Upload, Volume2, Wand2, X } from 'lucide-react';
import SectionHeader from '@/components/SectionHeader';
const tools = [{ label: 'Trim', icon: Scissors }, { label: 'Split', icon: Split }, { label: 'Text', icon: null }, { label: 'Stickers', icon: Sticker }, { label: 'Filters', icon: Sparkles }, { label: 'Transitions', icon: Wand2 }, { label: 'Music', icon: Music }, { label: 'Effects', icon: SlidersHorizontal }, { label: 'Speed', icon: Gauge }, { label: 'Volume', icon: Volume2 }, { label: 'Crop', icon: Crop }, { label: 'Rotate', icon: RotateCw }];
export default function VideoEditorView() { const [ratio, setRatio] = useState('16:9'); const [uploaded, setUploaded] = useState(false); const [playing, setPlaying] = useState(false); return <div className="space-y-6 animate-fade-in-up"><SectionHeader eyebrow="Content production" title="Video editor" description="Shape clips into scroll-stopping stories on any device." action={<div className="flex gap-1 p-1 rounded-lg bg-white/5">{['16:9', '9:16', '1:1'].map((item) => <button key={item} onClick={() => setRatio(item)} className={`px-3 py-1.5 rounded text-[10px] ${ratio === item ? 'bg-cyan-400/20 text-cyan-300' : 'text-gray-600'}`}>{item}</button>)}</div>} /><div className="grid lg:grid-cols-[1fr_300px] gap-5"><div className="space-y-4"><div className="glass-card p-3 sm:p-5"><div className={`relative mx-auto ${ratio === '9:16' ? 'max-w-[250px] aspect-[9/16]' : ratio === '1:1' ? 'max-w-[480px] aspect-square' : 'aspect-video'} rounded-xl bg-[#07070b] border border-white/5 flex items-center justify-center overflow-hidden`}><div className="absolute inset-0 bg-grid opacity-30" />{uploaded ? <div className="relative text-center"><div className="w-16 h-16 rounded-full bg-cyan-400/10 flex items-center justify-center mx-auto mb-3"><Film className="w-7 h-7 text-cyan-300" /></div><p className="text-sm text-gray-400">Your video preview</p></div> : <div className="relative text-center"><Upload className="w-8 h-8 text-gray-700 mx-auto mb-3" /><p className="text-sm text-gray-500">Drop a video here</p><label className="inline-block mt-3 px-4 py-2 rounded-lg neon-button text-xs cursor-pointer">Upload video<input type="file" accept="video/*" className="hidden" onChange={() => setUploaded(true)} /></label></div>}<span className="absolute bottom-3 right-3 px-2 py-1 rounded bg-black/60 text-[10px] text-gray-500">{ratio}</span></div><div className="flex items-center justify-center gap-4 mt-5"><button className="text-gray-500 hover:text-white"><Undo2 className="w-4 h-4" /></button><button onClick={() => setPlaying((value) => !value)} className="w-10 h-10 rounded-full neon-button-primary flex items-center justify-center">{playing ? <span className="text-xs">||</span> : <Play className="w-4 h-4 fill-current" />}</button><button className="text-gray-500 hover:text-white"><Redo2 className="w-4 h-4" /></button></div></div><div className="glass-card p-4"><div className="flex items-center justify-between mb-4"><div className="flex items-center gap-2"><Layers className="w-4 h-4 text-cyan-300" /><span className="text-xs font-semibold">Timeline</span></div><span className="text-[10px] text-gray-600">00:00 / 00:00</span></div><div className="h-20 rounded-lg bg-white/[0.02] border border-dashed border-white/10 flex items-center justify-center text-xs text-gray-700">Add media to timeline</div></div></div><aside className="glass-card p-4 h-fit"><div className="flex items-center justify-between mb-4"><h3 className="text-sm font-semibold">Edit tools</h3><button className="text-gray-600"><X className="w-4 h-4" /></button></div><div className="grid grid-cols-3 gap-2">{tools.map((tool, index) => { const Icon = index === 2 ? TypeIcon : tool.icon!; return <button key={tool.label} className="p-3 rounded-xl bg-white/[0.03] hover:bg-cyan-400/10 text-gray-500 hover:text-cyan-300 transition-colors"><Icon className="w-4 h-4 mx-auto" /><span className="block text-[9px] mt-2">{tool.label}</span></button>; })}</div><div className="mt-5 pt-4 border-t border-white/5 space-y-2"><button className="w-full neon-button py-2.5 rounded-xl text-xs inline-flex items-center justify-center gap-2"><Maximize className="w-3 h-3" /> Fit canvas</button><button className="w-full neon-button-primary py-2.5 rounded-xl text-xs">Export video</button></div></aside></div></div>;
}
function TypeIcon({ className }: { className?: string }) { return <span className={`${className ?? ''} text-sm font-bold`}>T</span>; }
